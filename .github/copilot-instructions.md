# Copilot Instructions

## Project Guidelines
- Repository preference: keep README.md at the solution root (used on GitHub); include it in project packaging via a relative path instead of moving it into the project folder.